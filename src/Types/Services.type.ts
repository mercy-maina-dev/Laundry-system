export interface Services {
    service_name: string;
     price_per_kg: number;
    description: string;
   
}

export interface NewServices {
    service_name: string;
     price_per_kg: number;
    description: string;        
}

export interface UpdateServices {
    service_name: string;
     price_per_kg: number;
    description: string;        
}